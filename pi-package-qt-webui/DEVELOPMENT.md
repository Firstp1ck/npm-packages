# Development guide: Qt WebUI

Contributor-only implementation, API, architecture, testing, and maintenance information.

[Back to README](README.md) · [Advanced user technical reference](TECHNICAL.md)

## Architecture

The package manifest loads `extensions/qt-webui-start.mjs` when Pi installs the package. Its `/qt-webui-start` handler passes `ctx.cwd` through the canonical `prepareLaunch()` path, spawns Quickshell with an argument array and `shell: false`, then detaches and unreferences the child so the Pi command returns without owning the window. Synchronous preparation errors and process spawn errors become bounded Pi notifications.

Three processes cooperate:

1. **Launcher** (`bin/qt-webui.mjs`, `lib/launcher.mjs`) resolves the package QML entry, the backend entry, and the CLI declared by the dependency-local `@earendil-works/pi-coding-agent` manifest, then spawns `quickshell` with an argument array, inherited stdio, `shell: false`, and the caller's working directory.
2. **Quickshell** owns the QML application. `qml/BackendBridge.qml` starts the backend with `QT_WEBUI_NODE_EXECUTABLE` and `QT_WEBUI_BACKEND_ENTRY`, writes protocol requests to its stdin, and reduces its events into QML state. QML never parses a raw Pi record and never starts Pi.
3. **Backend** (`lib/backend/main.mjs`) owns one Pi RPC child per tab through `tabs.mjs`, translates Pi records into bounded typed events tagged with the tab id, keeps a bounded transcript mirror per tab, renders Markdown, stores settings and window state, monitors portal appearance changes, lists sessions, browses directories, runs Git, sends notifications, opens links, and reaps every child on shutdown.

The Pi package exposes only import-conditioned exports, so the launcher resolves its public module with ESM resolution, walks to the matching package manifest, and uses a `createRequire` rooted there to confirm package identity. It reads the `bin.pi` declaration instead of assuming a dependency layout or searching `PATH`.

### Backend modules

| Module | Responsibility |
|---|---|
| `lib/backend/protocol.mjs` | Protocol version, every numeric limit, request validation, frame constructors, link scheme policy. |
| `lib/backend/jsonl.mjs` | LF-only JSONL reader with a per-frame byte cap; oversized frames are discarded without buffering. Node `readline` is not used because it also splits on U+2028/U+2029. |
| `lib/backend/pi-session.mjs` | Pi child ownership and the state machine ported from the former `PiBridge.qml`: startup readiness, prompt acceptance and reconciliation, abort-before-start, provider errors, restart, message parts, tool lifecycle, extension dialogs. |
| `lib/backend/markdown.mjs` | Bounded Markdown-to-blocks renderer producing whitelisted Qt StyledText. |
| `lib/backend/settings.mjs` | XDG settings store with validation, 0700/0600 permissions, and atomic replace. |
| `lib/backend/appearance.mjs` | Bounded XDG portal color-scheme parsing and a supervised `gdbus monitor` subscription that retains the last valid value. |
| `lib/backend/themes.mjs` | Public Pi package-resource discovery with no-install missing-package handling, project-trust filtering, bounded Pi JSON parsing, semantic palette mapping, contrast repair, requested/effective/fallback state, and active-directory watching. |
| `lib/backend/process-tree.mjs` | Detached process-group spawn, SIGTERM→SIGKILL escalation, synchronous kill for fatal paths. |
| `lib/backend/desktop.mjs` | `notify-send` and `xdg-open` wrappers using argument arrays only. |
| `lib/backend/highlight.mjs` | Bounded regex tokenizer for fenced code; emits `[kind, escapedText]` pairs, never colors or markup. |
| `lib/backend/store.mjs` | Generic private JSON document store (validate on read, atomic replace, 0700/0600) plus the XDG state directory. |
| `lib/backend/state.mjs` | Window state in `$XDG_STATE_HOME/qt-webui/state.json`: drafts, recent and pinned directories, recent actions, tabs, separately bounded private manual and automatic settled-session identities, and bounded restore-grace timestamps keyed by the same hashes. |
| `lib/backend/sequences.mjs` | Saved prompt sequences in the config directory with per-entry validation and stable ordering. |
| `lib/backend/attachments.mjs` | In-memory composer attachments: confinement, size and type checks, exact-once consumption, message composition. |
| `lib/backend/workspace.mjs` | Workspace confinement helpers and the cached path index (`git ls-files` or a bounded walk) behind `@` completion. |
| `lib/backend/tabs.mjs` | Tab registry: one `pi-session` per tab with its own attachments, path index, and transcript mirror; tagged events; unread badges; external persisted projection and stale-child state; select/close/rename/move; restart continuity; saved-layout restore. |
| `lib/backend/session-sync.mjs` | Bounded session-root/project watcher set, two-second polling fallback for up to eight open paths, revision acknowledgment, and stable complete-snapshot loading through Pi's public `SessionManager` API. |
| `lib/backend/transcript.mjs` | Backend transcript rows: the live mirror (`createTranscriptMirror`) and the sole persisted-message conversion path, `rowsFromHistory`, including interruption detection. |
| `lib/backend/sessions-index.mjs` | Bounded listing of Pi's persisted sessions for one folder or every project under the active agent home, with real-path deduplication, stable recency ordering, pages, and a per-file scan budget. |
| `lib/backend/directories.mjs` | Directory listing and folder creation for the workspace picker (bounded, hidden opt-in, Git hints). |
| `lib/backend/git.mjs` | `runGit` (argument arrays, timeout, bounded output), repository/branch/worktree inspection, `planWorktree`, `createWorktree` with rollback, `removeWorktree`. |
| `lib/backend/resources.mjs` | Adapter for Pi Web UI-owned global/exact-model tool and skill profiles plus Qt-owned sampling, including validation, legacy migration, null-versus-empty semantics, and session → model → global → Pi-default effective resolution. |
| `lib/backend/sampling.mjs` | Sampling ranges, provider-interface capability reasons, supported-value filtering, and exact provider payload translation. |
| `lib/pi-extension/qt-webui-helper.mjs` | Pi-side capability inventory and session-history persistence; applies effective enabled tool/skill lists and supported sampling immediately before provider requests. |

## Backend protocol (version 1)

Frames are JSON objects terminated by `\n`. Every frame carries `v: 1`. Requests flow from QML to the backend; the backend answers each request exactly once by `id` and emits unsolicited events with a monotonically increasing `seq`.

Request: `{"v":1,"id":"q-7","type":"prompt","message":"…","mode":"send","tab":"tab-1"}` — session-scoped requests may carry `tab`; without it the active tab is used. Global requests (settings, links, sequences, drafts, directories) ignore it.
Response: `{"v":1,"kind":"response","id":"q-7","ok":true,"data":{…}}` or `{"v":1,"kind":"response","id":"q-7","ok":false,"error":{"code":"busy","message":"…"}}`
Event: `{"v":1,"kind":"event","seq":42,"type":"part.render",…}`

Event payloads must not use the reserved frame keys `v`, `kind`, `type`, or `id`; `makeEvent` throws otherwise (this caught the `part.begin` `kind` collision during development).

### Requests

| Type | Fields | Result |
|---|---|---|
| `hello` | — | protocol version, backend pid, cwd, limits, session snapshot, settings, queue stats |
| `prompt` | `message`, `mode` (`send`, `steer`, `followUp`), optional `attachments` (ids) | `{mode, messageId}` after Pi accepts; `busy` while a `send` is active; `pi_error` when Pi rejects; attachments are consumed only after the prompt is known to be acceptable, text attachments become labelled fenced blocks below the message, images travel in Pi's `images` field |
| `commands_list` | — | `{commands, omitted}` from `get_commands`: `name`, `description`, `source` (`extension`, `prompt`, `skill`), `location`, `path`; bounded to 512, deduplicated by name |
| `path_complete` | `query` (≤ 256 characters) | `{suggestions: [{path, directory}], total, truncated, source}`; ranked basename prefix → path prefix → substring → subsequence, at most 50 |
| `draft_get` / `draft_set` | `key` (session file or workspace), `text` (≤ 8,192) | the stored draft; an empty text deletes it; 64 drafts are kept, oldest evicted |
| `sequences_list` / `sequence_save` / `sequence_delete` / `sequence_move` / `sequence_run` | `sequenceId`, `name` (≤ 64), `entries` (1–16 × ≤ 8,192), `delta` (±1) | the sequence list after each mutation; `sequence_run` sends entry 1 as a prompt and the rest as follow-ups and returns `{sent, queued}`; `stale_request` for unknown ids, `limit_exceeded` beyond 32 sequences |
| `attachment_add` / `attachment_update` / `attachment_remove` | `path` (absolute), `granted` (picker-chosen), `attachmentId`, `text` | the attachment list; `rejected` outside the workspace unless granted, for symlinks that escape, non-files, invalid UTF-8, or image signature mismatches; `limit_exceeded` above 8 attachments, 256 KiB text, or 5 MiB images |
| `tabs_list` | — | `{tabs: [{id, cwd, name, sessionFile, sessionName, statusKind, statusText, ready, active, activityState, unread, needsInput, pid}], activeTab, maxTabs}`; `activityState` is `blocked`, `working`, `done`, or `idle` |
| `tab_open` | optional `cwd`, `sessionPath`, `name` | opens and selects a tab (`limit_exceeded` above 8; `rejected` for a missing folder); resumes `sessionPath` once Pi is ready; returns `{tab, session, attachments}` (a snapshot) |
| `tab_select` | `tab` | emits `tabs.update`, then `transcript.reset` and one `transcript.row` per mirrored row, then answers with the snapshot |
| `tab_close` | `tab`, `force` | `busy` while the tab's run is active unless `force`; stops the Pi tree; closing the active tab returns `activeTab: ""` and does not select or create a replacement; closing an inactive tab preserves the selection |
| `tab_rename` / `tab_move` | `name` (≤ 64) / `delta` (±1) | rename also calls `set_session_name` (a failure is a notice, the local name still changes); move reorders |
| `sessions_list` | optional `scope` (`workspace` or `all`), `offset` | Workspace scope returns the legacy `{sessions, omitted, directory, current, cwd}` list. All scope returns a 200-candidate page with `scope`, `offset`, `nextOffset`, `total`, and rows containing `path`, `id`, `name`, `cwd`, timestamps, counts, preview, scan status, and `settled`; paths are canonicalized and deduplicated while invalid files are omitted without stopping page progress. |
| `session_settled` | `sessionPath` (absolute `.jsonl`), `settled` (boolean) | Validates that the file belongs to the managed session tree, refuses a newly settled matching active tab with `busy`, persists the reversible organization flag, emits `sessions.changed`, and returns `{path, settled}`. It never writes the Pi session file. |
| `session_switch` | `sessionPath` (absolute `.jsonl`) | `switch_session`, then `get_messages` replayed as `transcript.reset` + `transcript.row` events, then `get_state`; `busy` while active; `pi_error` when an extension cancels; returns `{sessionFile, sessionName, rows, messageCount, interrupted}` |
| `session_new` | — | `new_session`, `transcript.reset`, fresh state; `busy` while active |
| `directory_list` / `directory_create` / `directory_pin` | `path`, `showHidden` / `path`, `name` / `path` | `{path, parent, entries: [{name, path, hidden, git}], hiddenCount, omitted, home, recent, pinned}`; creation validates the name and refuses existing folders; pin toggles the pinned list |
| `open_path` | `path` (absolute) | `xdg-open` for an existing regular file (`rejected` otherwise); suppressed in smoke mode; the shell confirms the exact path first |
| `session_stats` | — | `get_session_stats` normalized to `{userMessages, assistantMessages, toolCalls, totalMessages, tokens: {input, output, cacheRead, cacheWrite, total}, cost, context: {tokens, contextWindow, percent} | null}` |
| `recent_action` | `action` (identifier ≤ 128) | pushes the key to the state store's recents (20 kept, most recent first) and returns the list; `hello` also returns it |
| `diagnostics` | — | backend pid, uptime, RSS, queue statistics, tab list, recent actions, file paths, limits |
| `worktrees_list` / `worktree_plan` / `worktree_create` | `branch`, `base`, `path`, `confirmed: true`, `openTab` | plan returns `{root, branch, path, base, detachedBase, nested, problems}` without changes; create requires `confirmed` (`invalid_request` otherwise), runs `git worktree add -b`, rolls back on failure, and opens the new tab when `openTab` is not false |
| `abort` | — | `null`; `not_running` when idle |
| `state` | — | runtime metadata from `get_state` |
| `restart` | — | terminates the Pi tree and starts a new child |
| `extension_response` | `requestId` plus exactly one of `value`, `confirmed`, `cancelled` | echoes the answer; `stale_request` if not pending; `invalid_request` for a value outside the offered options or a wrong shape |
| `settings_get` / `settings_set` | `values` object with schema-checked keys; `selectedThemeName` is empty or a bounded external theme name, `sessionSettleDays` is an integer from 1 through 3,650, and `modelOrder` is a deduplicated list of at most 256 exact `provider/model-id` strings | current settings and file path |
| `themes_list` | — | refreshes installed and configured theme resources and returns one bounded state with `requested`, `effective`, `fallbackReason`, `inventory`, `diagnostics`, and the complete palette or `null` |
| `theme_select` | `selection` (`{kind: "builtin" | "external", name}`) | validates against the latest inventory, writes only Qt WebUI settings, and returns the complete theme state; stale external identities return `stale_request` |
| `open_link` | `url` | `xdg-open` result; rejected for disallowed schemes |
| `notify` | `title`, `body` | `notify-send` result |
| `models_list` | — | `{models, omitted, scope, current}`; an explicit helper-reported `ctx.scopedModels` list is intersected with the RPC catalogue in Pi order, otherwise Pi's empty-scope semantics fall back to all available models; models add optional `pinnedThinkingLevel`, are normalized and deduplicated by `provider/id`, and are bounded to 256. Idle requests refresh scope; active requests use only an established cache and never send a helper prompt into the run. |
| `model_set` | `provider`, `modelId` | `{model, thinkingLevel, resources}` after `set_model`, a fresh `get_state`, and a fail-closed resource refresh; core model success remains successful when `resources.available` is false |
| `model_cycle` | — | `{changed, model, thinkingLevel, scoped, resources}`; `changed: false` when Pi returns no model |
| `thinking_levels` | — | `{levels, current}`; only levels from `THINKING_LEVELS` survive, in canonical order, `["off"]` when empty |
| `thinking_set` | `level` (one of `THINKING_LEVELS`) | `{level, resources}`; `busy` while active; `pi_error` when the model rejects it |
| `thinking_cycle` | — | `{changed, level, resources}` |
| `resources_state` | — | `{available, model, thinkingLevel, profiles, sessionDurability, effective, tools, skills, sampling, problems, path, sharedPath}`; `path` is Qt sampling storage, `sharedPath` is Pi Web UI tool/skill storage, `sessionDurability` is `{durable, reason}`, and `available: false` carries a bounded error when helper/capability state is unavailable |
| `tools_set` | `scope` (`session`, `model`, `global`), `enabledTools` (`string[]` or `null`) | validated applied resource state; `null` inherits and `[]` intentionally enables none |
| `skills_set` | `scope`, `enabledSkills` (`string[]` or `null`) | validated applied resource state; public and persisted profiles always use enabled names (the helper alone translates to Pi's internal disabled set) |
| `sampling_set` | `scope`, `params` (partial sampling object or `null`) | validated applied resource state; null values remove keys, unsupported values remain stored/effective but are omitted from `sampling.applied` |
| `compact` | optional `instructions` (≤ 1,024 characters) | `{tokensBefore, estimatedTokensAfter, summary}` (summary ≤ 512 characters); the session is `active` with status `Compacting…` until Pi answers, so `prompt`, model, thinking, and further `compact` requests get `busy`; failure clears the busy state and posts a notice |
| `shutdown` | — | graceful shutdown, exit 0 |
| `debug_crash` | — | smoke mode only: raises an uncaught exception to exercise the fatal path |

Error codes: `invalid_request`, `unsupported_version`, `unknown_request`, `limit_exceeded`, `duplicate_request`, `busy`, `not_ready`, `not_running`, `pi_error`, `stale_request`, `timeout`, `rejected`, `internal_error`.

### Events

Every event a Pi session produces carries `tab`; `backend.*`, `settings.changed`, `themes.changed`, `sessions.changed`, `events.dropped`, global notices, and `tabs.update` do not. `themes.changed` carries the same complete state as `themes_list`; QML generation checks discard older list responses before assigning the object once. `sessions.changed` carries the canonical session `path` and confirmed `settled` value after settlement persistence, or `reason: "filesystem"` when the monitor invalidates the catalog. A successful profile mutation emits `resources.changed` with `tab`, `scope`, `field`, and the full validated applied `state` for every affected tab. `tabs.update` (`{tabs, activeTab, maxTabs}`) is sent only when a summary changed, and always before the transcript replay of a newly selected tab so the client switches first; `transcript.reset` clears the tab's transcript and `transcript.row` (`row` in the client's row shape) rebuilds it, one row per frame. Session events: `backend.ready`, `backend.closing`, `backend.fatal`, `backend.backpressure`, `events.dropped`, `notice`, `settings.changed`, `pi.started`, `pi.status` (`statusKind`, `text`, `ready`, `active`), `pi.error` (empty message clears), `pi.runtime` (`provider`, `modelId`, `modelName`, `modelReasoning`, `thinkingLevel`, `sessionId`, `sessionName`, `sessionFile`, `messageCount`; emitted after every state read and after model or thinking changes, which merge into the existing session identity), `pi.exit`, `message.user` (`text`, `mode`, `attachments`: bounded file names), `message.begin`, `part.begin` (`partKind` `text` or `thinking`), `part.render` (`text`, `blocks` for text parts, `final`, `truncated`), `part.remove`, `message.end`, `tool.start` (`summary` is a bounded plain-text argument digest), `tool.update`, `tool.end` (`ok`, `durationMs`, `output`, `error`), `run.start`, `run.end` (`ok`, `aborted`), `queue.update`, `extension.request`, `extension.answered`, `extension.cancelled`, `extension.notify`, `extension.status` (`key`, `text`, and `chips`: a `setStatus` whose text is a `firstpick.git-footer-status.footer` version 1 JSON payload becomes up to 18 bounded plain-text chips from its `main` and `meta` arrays; any other JSON object with a `type` becomes its `title` as `text` and `description` as `hint`; other text stays as `text`), `composer.setText`, `window.title`.

The tab registry computes `activityState` rather than leaving QML to infer it. Pending extension input during an active run has `blocked` priority, other active runs are `working`, unseen background completion is `done`, and all other open tabs are `idle`. `run.start` clears stale completion, while `tab_select` acknowledges `done`. This state is ephemeral and does not replace `statusKind` or `statusText` process-error evidence.

`part.render` is emitted at most every 80 ms per streaming part and once more with `final: true` after `message_end`, whose content is authoritative. Parts that streamed but are missing from the final message are removed.

The backend starts `session-sync` after restored paths are registered and stops it during graceful, forced, and fatal cleanup. Root and immediate-project filesystem events coalesce catalog invalidation; open paths are also stat-polled every two seconds. Both path registration and projection pass through `managedSessionPath()`, so only regular `.jsonl` files whose real paths remain under the active sessions root are read; escaping file and directory symlinks are excluded. Pi may publish a future session path before its first durable write creates the file. Registry-owned paths that fail managed validation as unavailable stay in a set bounded by the eight-tab registry and are revalidated after catalog filesystem events; they enter the monitor only after the same realpath and regular-file check succeeds. A changed path is read into an isolated copy and projected by Pi's compaction-aware `buildSessionContext()`, then converted only by `rowsFromHistory`. Semantic row comparison ignores generated row/message ids, streaming flags, live-only tool duration, and the persisted branch leaf id. Equal rows plus durable session metadata acknowledge the revision without replay. A different complete snapshot replaces the mirror, replays only an active tab, increments unread only on an inactive tab, updates persisted runtime metadata, and marks the child stale. Missing, partial, malformed, unstable, and unreadable snapshots remain pending without changing the mirror; repeated failures of one revision use exponential retry backoff from two seconds through one minute, while a new revision retries immediately. Warnings are bounded by operation and error class.

`SESSION_MUTATION_REQUESTS` is the centralized stale-write fence for prompt/steer/follow-up, sequence, rename, compaction, model, thinking, resource-state/apply, tool, skill, and sampling paths. Before dispatch, a stale tab re-enters the existing `switch_session` → `get_messages` → `get_state` flow for its current file. Concurrent guards on one tab share its serialized preparation promise, and a generation check prevents an older rebind from clearing a newer external revision. Resource transactions repeat the guard for every affected tab. Cancellation or failure leaves stale state set and rejects the requested mutation. Explicit session switch and successful new-session replacement clear it; restart retains the current path until startup resume clears it; close drops the tab and all backend reconciliation state for its now-unowned path. Projection defers while the child is active, preparing, or owned by a mutation or exclusive operation. After asynchronous loading it rechecks the pending revision, path ownership, tab generation, and busy state before applying, then retries deferred work on `run.end`, the first ready/idle status, or a later poll.

Resource mutations lock every affected tab and recheck that each is idle. Global changes target every open tab; exact-model changes target every tab currently using that provider/model. The backend reads and validates each helper state plus the latest canonical settings, applies the prospective effective profile to every target, and only then commits. Tool and skill writes call Pi Web UI's `updateWebuiSettings()` and `setExactModelProfile()` modules against the latest locked snapshot; sampling writes remain in Qt's `resources.json`. The selector submits only names in its visible inventory, and the backend supplies that inventory so `preserveUnavailableResourceNames()` retains configured names that are not currently loaded; `helperEffective()` filters the committed selection against each tab's own inventory before applying runtime state. Responses use the combined snapshot returned by the commit rather than a fallible post-commit store read. If latest-snapshot merging changes an effective selection, the backend reapplies that committed selection to each affected tab and validates the result before reporting success. An apply or pre-commit store failure rolls every attempted helper back; rollback failure is returned explicitly with an inconsistent-state warning. A post-commit reconciliation failure is reported as saved but runtime-inconsistent because the canonical commit cannot honestly be rolled back. One centralized per-tab guard fences prompts, saved sequences, compaction, Pi restart, tab close, session switch, and new-session requests until reconciliation and any rollback settle. The lifecycle handlers hold the same guard while they run, so a broader profile transaction cannot start across them either.

`lib/backend/resources.mjs` combines two ownership domains behind its asynchronous `read()`, `profileFor()`, and `update()` API. Pi Web UI's settings envelope owns nullable global and exact-model enabled-name lists, locking, latest-snapshot merges, atomic replacement, and the `PI_WEBUI_SETTINGS_FILE` override. Qt's version-2 `resources.json` owns sampling and retains legacy tool/skill values plus its earlier bounded migration marker for compatibility. The first valid legacy read migrates only canonical-null fields under Pi Web UI's lock, preserves canonical values and unrelated settings, retains all legacy data, and records migration completion in the same canonical atomic update so a failure cannot expose migrated values without the no-retry marker. Malformed, oversized, or partially invalid local data is reported and left untouched rather than being rewritten during migration. Exact-model migration and writes verify that Pi Web UI retained the requested profile within its supported bound, and in-memory model identities use unambiguous provider/model tuples while legacy slash-keyed sampling remains readable. `resourceResult()` keeps `path` as the Qt sampling file and adds `sharedPath` for the canonical settings file; diagnostics likewise retain `resources` and add `sharedResources`.

`pi-session.helperCall` observes the helper-notify promise before sending the command prompt. A helper answer wins even if it precedes Pi's prompt acknowledgement; a prompt rejection/timeout rejects and clears the helper leg; helper timeout or error clears the pending Pi command; Pi exit rejects both through their normal handlers. Successful helper answers also refresh the bounded scoped-model cache. The helper writes session tools and skills as Pi Web UI's `webui-tools-config` and `webui-skills-config` version-2 explicit/inherit entries. It reads legacy `disabledSkills` and uses a legacy `qt-webui-resources` field only when no shared entry exists; that Qt entry remains canonical for session sampling. It persists before changing in-memory state. A real append failure is returned as an error; only `sessionManager.isPersisted() === false` skips the append and returns `durable: false` with a user-facing reason.

### Limits

All numbers live in `LIMITS` in `lib/backend/protocol.mjs`; `tests/qml-contract.test.mjs` asserts the QML copies match.

| Budget | Value |
|---|---|
| Inbound / outbound frame | 256 KiB / 1 MiB (Pi records: 4 MiB) |
| Outbound queue before backpressure | 4 MiB or 2,000 unflushed records |
| Pending requests (each direction) | 64; request ids ≤ 96 characters |
| Request timeouts | prompt 30 s, restart 20 s, abort/state 10 s, others 5 s; the client also enforces the same deadlines |
| Pi startup readiness | 15 s (`QT_WEBUI_PI_STARTUP_TIMEOUT_MS` test seam) |
| Prompt reconciliation | 150 ms after acceptance without `agent_start` |
| Shutdown grace | 3 s SIGTERM, then SIGKILL |
| Transcript | 80 rows, 8,192 characters per text/thinking part, 64 parts per message |
| Tool cards | 64-character name, 256-character summary, 4,096-character output |
| Errors / notices | 512 characters; runtime metadata 160 characters |
| Markdown | 8,192 input characters, 200 blocks, nesting depth 4, 50 table rows × 12 columns, 200 list items |
| Dialogs | 16 pending, 64 options × 256 characters, 256-character title, 4,096-character message, 16 KiB values |
| Settings file | 64 KiB |
| Themes | 128 files from 256 resources, 128 KiB per file, 64-character names, 128 variables, depth 16, 64 diagnostics |
| Models and thinking | 256 models; provider 64, model ID 128, model name 96 characters; 8 thinking levels |
| Compaction | 1,024-character instructions, 512-character summary, 120 s request timeout |
| Highlighting | 8,192 characters and 4,000 tokens per code block; over either, the block stays plain |
| Drafts and state | 8,192 characters per draft, 64 drafts, 20 recent entries per list, 512 KiB state file |
| Sequences | 32 sequences × 16 entries, 64-character names, 1 MiB file |
| Commands | 512 commands, 64-character names, 256-character descriptions |
| Attachments | 8 per prompt, 256 KiB text, 5 MiB images, 128-character names |
| Workspace index | 20,000 entries, depth 16, 50 suggestions, 256-character query, 5 s cache, 5 s `git ls-files` timeout, 8 MiB output |
| Tabs | 8 tabs, 64-character names, one transcript mirror of 80 rows each, 16 saved tabs in state |
| Sessions | 200 candidates per global page, offset ≤ 1,000,000, 1 MiB scanned per catalog file, 160-character previews, 2,048 manual settled identities, 2,048 automatic settled identities, and 2,048 restore-grace entries in private state; automatic delay 1–3,650 days; 256 project watchers and 8 open-file polls at a 2 s cadence |
| Directories | 500 entries per listing, 255-character folder names |
| Git | 10 s per command (60 s for `worktree add`), 4 MiB output, 64 worktrees, 128-character branch names |
| Palette and events | 128-character action keys, 20 recent actions, 200 notices kept in the client |
| Resource profiles | 512 tool or skill names, 64 exact-model profiles, 256 KiB file, 512 KiB helper response, 10 s helper timeout |

### Slow consumers

Coalescable events (`part.render` before `final`, `tool.update`) are dropped while the outbound queue is over budget and reported later through `events.dropped`. Essential events are never dropped: when a write leaves the queue over budget the backend pauses the Pi child's stdout and resumes it on `drain`, emitting `backend.backpressure`. Node emits `drain` before invoking write callbacks, so the engagement condition is bytes-only and requires the write to have returned `false`.

## Markdown policy

`renderMarkdown` produces `heading`, `paragraph` (with `quote` flag), `code`, `listItem`, `table`, `rule`, and `notice` blocks. Inline text becomes StyledText built only from escaped text and the tags `<b>`, `<i>`, `<s>`, `<tt>`, `<br>`, and `<a href>`. Raw HTML is escaped, images become `[image: alt]` placeholders, and links keep only `http`, `https`, and `mailto` targets without credentials; other links stay as their literal Markdown. QML renders `styled` with `Text.StyledText`, code and tool output with read-only plain-text editors, and everything else as `Text.PlainText`. Link activation always goes through `LinkDialog` and then the backend's `open_link`.

Code blocks additionally carry `tokens` (`[kind, escapedText]` pairs from `highlight.mjs`, or `null` for unknown languages and oversized blocks). `MarkdownBlocks.styledCode` wraps them in `<pre>` with `<font color>` taken from `Theme.syntaxColor(kind)`; `<pre>` is required because `Text.StyledText` collapses newlines and runs of spaces outside it. The highlighted view is a `Label`, so a **Select text** toggle swaps in the plain `TextEdit`; copy uses `block.text` in both views.

## Lifecycle and process ownership

Each tab's Pi child is spawned with `detached: true` so it leads its own process group; `terminateProcessTree` signals the group, escalates to SIGKILL after the grace period, and sweeps the group again once the leader exits. Shutdown runs on stdin EOF (Quickshell exited), `SIGINT`, `SIGTERM`, `SIGHUP`, the `shutdown` request, and stdout `EPIPE`; the same owner stops every session watcher and poll timer before exit. Uncaught exceptions emit `backend.fatal`, synchronously initiate monitor cleanup, kill the tree, and exit 70. A relative or missing Pi entry exits 64 before starting anything.

If the backend itself is killed with SIGKILL it cannot signal anything; Pi then sees EOF on stdin and is responsible for stopping its own tool processes, which the fixture models by terminating its grandchild on `end`. `tests/backend-lifecycle.test.mjs` proves each path with a fixture grandchild (`sleep 300`).

Shutdown stops every tab (`registry.stopAll`); the forced path kills every child's process group. The state store records `activeTab: -1` for an intentionally empty selection. At startup `registry.restore()` reopens saved tabs whose folders still exist and resumes their session files once Pi is ready. The `-1` sentinel leaves them unselected. Fresh or older state keeps the previous behavior: the registry adds a tab for the launch directory when none matches and selects it. Each path emits one final `tabs.update`. A per-tab `restart` records the tab's session file as `pendingResume` so the new Pi child resumes it. The registry also retains explicit switch targets until completion; these pending and transition paths are internal and never enter tab summaries sent to QML.

In QML, the bridge treats a backend exit as an error state, fails every pending request, cancels dialogs, clears the tab list, and offers **Restart**, which starts a fresh backend that restores the saved tabs. During `Qt.quit` the bridge sends `shutdown` first and stops the process after a short grace period.

## QML structure

- `qml/shell.qml` — opaque window with a pointer- and keyboard-resizable workspace rail whose 148 px minimum is also the only main-workspace reserve, global session navigation, compact workspace header, centered 820 px conversation column, search, transcript follow-output logic with a floating Latest resume control, queues, notices, normal-flow composer with a wrapping response/view control strip and anchored more-options/model/thinking drop-ups plus an on-demand status overlay beneath it, dialogs, and the smoke driver loader. The 560 × 520 minimum remains the responsive boundary.
- `qml/BackendBridge.qml` — backend process, request correlation with client-side deadlines, event reduction, transcript `ListModel`, paged/deduplicated global session-catalog state, confirmed single and sequential bulk settlement mutations, open-tab reuse/new-tab resume, dialog queue, settings, notifications, clipboard, and fail-closed resource state/mutations.
- `qml/Theme.qml` — the only palette and visual-scale owner. It rejects partial external snapshots, then switches every semantic color through one complete palette while keeping built-in colors and all non-color tokens. It publishes semantic palette, frame, filled-button, interaction, success, spacing, typography, tracking, radius, desktop-geometry, focus, selection, and motion tokens. The built-in branches use violet-charcoal or softly violet bases with a periwinkle structural accent; green is reserved for success. Both remain flat, opaque, monospace-first, and square when no compositor geometry is available. Automatic mode uses the live portal value before Qt style hints; saved Light/Dark overrides win, and reduced motion makes app-owned transitions immediate.
- `qml/components/` — `AppButton` (compact hover/pressed/active/disabled/focus states), `StatusBadge` (rectangular tracked status punctuation), `NoticeBar`, `Composer` (flat strongly framed surface, tracked prompt/mode labels, run modes, attachment chips, completion), `CompletionPopup`, `DropUpPicker` (bounded non-modal picker positioned above a control, with optional filtering, explicit keyboard selection, opt-in drag/keyboard reordering, outside/Escape dismissal, and focus return), `StatusOverlay` (bounded non-modal grouped status details with full wrapped values, inline descriptions, scrolling, and trigger focus return), `SearchBar`, `EmptyState` (separate no-selection and empty-transcript actions with an accessible text landmark), `TranscriptRow` (quiet full-width assistant rows, tracked role labels, and bounded user/thinking surfaces), `MarkdownBlocks`, `ToolCard`, `SessionList` (Working plus default-expanded/collapsible Settled, merged open-tab context, five-minute deferred activity sorting, minute-refreshed compact activity/date labels, transient case-insensitive title/identifier/folder filtering, bounded lists, idle guards, threshold-gated floating Settle All, restore, close/new/folder actions), and the retained reusable `TabStrip`.
- `qml/dialogs/` — `AppDialog` (modal, focus containment, Escape, focus return), `ExtensionDialog` (exactly-once answers), `LinkDialog`, `PickerDialog` (generic filterable modal list used by the command palette and session choices; `picked` is emitted once before `close()`, and navigation never picks), `WorktreeDialog` (editable suggested type plus a separate name, combined and validated before planning), `ResourceProfilesDialog` (explicit scope/section selection, bounded enabled-name lists, inheritance/effective source labels, capability-disabled sampling fields, explicit save), `SequencesDialog` (list and edit modes; Run is the only action that sends, Delete arms and then confirms), `TextEditDialog` (bounded multi-line editor used for text attachments), `EventsDialog` (filters, repeat grouping, copy, clear over the bridge's notice model), `DiagnosticsDialog` (plain-text report from `diagnostics` plus client counters), `DirectoryDialog` (folder picker; only `choose`/`chooseCurrentEntry` emit `chosen`), `ConfirmDialog` (destructive confirmations focus Cancel first; answers once), `InputDialog` (validated single-line input; never submits invalid text).

`SessionList` and `TabStrip` consume the registry's activity value directly. Both use lowercase labels and the existing semantic colors for blocked, working, done, idle, and orthogonal process errors. Their tooltips and accessible names include activity plus startup, error, and pending-input details without deriving completion from unread state.

The global session catalog starts after `hello`, requests `sessions_list {scope: "all", offset}` sequentially until `nextOffset` is null, and deduplicates by canonical path before replacing the visible array. A generation token discards superseded page callbacks without partially replacing the previous catalog. Global pages explicitly opt out of origin-tab callback dropping; the legacy `listSessions()` request remains session-scoped for the in-place workspace picker. `sessions.changed`, session-file changes, and run completion pass through one 500 ms coalescing timer. Settlement rows move only after a successful `session_settled` response, while the event-triggered refresh reconciles external changes. `settleAllSessions()` snapshots loaded unsettled paths, excludes active tabs, and calls the existing mutation sequentially so the client never spends more than one backend request slot on the batch. Each confirmed response updates the catalog immediately, which hides the floating button when the raw saved-session count reaches 100. Backend exit clears the bulk-pending guard; normal completion reports settled, failed, and active-skip counts. `openCatalogSession()` selects a matching open tab or calls `tab_open` with the saved `cwd` and `path`, preserving the eight-tab guard.

`SessionList` derives both visible groups from the unchanged loaded catalog. On first load, it commits each saved row's `modified` timestamp as the transient ordering key. A later timestamp stays pending while its age is below five minutes, so catalog refreshes cannot move that row. The minute timer commits it at the boundary and sorts newest first. Further activity moves the boundary forward, and each reconciliation drops keys for sessions no longer in the catalog. This state is client-only and resets at startup. The same timer refreshes activity labels: minutes or hours below one day, whole elapsed days through 30 days, then a zero-padded local `dd.mm.yyyy` date. Temporary unsaved rows have no activity label. The component trims and lowercases the transient query, filters enriched rows against title, identifier, and workspace path text, and returns the source arrays unchanged for an empty query. The query never crosses the QML bridge or enters persisted settings.

For each all-scope page, the backend captures one current time and one stable registry snapshot containing every tab's current, startup-resume, and explicit-switch path. It canonicalizes that snapshot through the managed-session boundary and uses the resulting private identity map for both open-row association and reconciliation. Non-settled rows whose `modified` age is at least `sessionSettleDays × 24 hours` are added to the separate automatic settled collection, excluding every open identity and every unexpired restore-grace entry. The manual and automatic collections are each bounded at 2,048, and their union determines catalog grouping, so automatic aging cannot consume manual Settle capacity. Restore removes the identity from both collections. A valid restoration timestamp in the future remains protected with zero elapsed grace after a backward wall-clock change; otherwise grace expires exactly at the current threshold. Automatic settlement clears expired grace and persists only hashed identities and timestamps; it does not emit `sessions.changed`, because the current response already carries the authoritative settled value and another event would create a refresh loop. Manual Settle clears grace. Reconciliation remains page-bounded and writes state only when the page adds settlement changes.

`BackendBridge.applySettings()` accepts only confirmed in-range `sessionSettleDays` values. `setSessionSettleDays()` repeats the integer bounds, sends `settings_set`, leaves bridge state unchanged on local rejection, cancellation, backend error, or malformed confirmation, then applies the confirmed settings and calls `refreshSessionCatalog()`. The shell exposes the current value in **More options** and the command palette and reuses `InputDialog`; only its submitted signal reaches the setting mutation. The dialog validator accepts decimal whole-number text from 1 through 3,650 and warns that lowering the value may settle closed inactive sessions.

Tabs in the bridge: only the active tab is materialized, and an empty `activeTabId` is a valid no-selection state. `request()` tags every frame with `activeTabId` and records that origin for session-scoped callbacks; `settlePending()` removes a response but does not invoke its state-mutating callback after another tab becomes active. `handleEvent` routes events whose `tab` differs to `handleInactiveTabEvent` and never lets them touch the view. The inactive handler keeps in-window notices for input requests, finished runs, errors, and exits, but the shared desktop notification guard suppresses requests whose originating tab maps to a settled catalog entry. The same guard formats every delivered notification with the originating tab name, Pi session name, or workspace-label fallback before any event-specific detail. `tabs.update` with a different `activeTab`, including `""`, calls `beginTabSwitch`, which runs `resetTabState` (transcript, status, runtime, queues, attachments, commands, footer status, and the local dialog queue — dropped without answering, because they stay pending in the backend) and emits `tabSwitched`; the replay events then rebuild the transcript and the request's response (`applySnapshot`) restores status, runtime, error, queues, footer records, attachments, and pending dialogs (`enqueueDialog` skips ids already queued). The shell saves the previous tab's composer text under its draft key before the new tab's draft loads (`handleDraftKeyChanged`) and asks `ConfirmDialog` before force-closing a working tab. With no active session, it hides the workspace header, composer, response controls, and session status while `EmptyState` offers **New session** and **Open folder**. Worktrees are created only from `confirmAccepted` after `worktree_plan` showed the path.

Composer completion: `completionContext()` derives the token under the cursor (`/` in the first word → command, `@word` → path) and emits `completionRequested(kind, query)`; the shell answers with command suggestions from the bridge's cached `commands_list` or, after a 120 ms debounce, `path_complete`. `acceptCompletion` only edits the text (`/name ` or `@path` with a trailing `/` for directories) and `dismissCompletion` suppresses the same token until it changes. While the list is open, Up/Down/Tab/Enter/Escape are consumed before the send handling. A QML pitfall found here: inside `onItemsChanged` a derived binding such as `count: items.length` still holds the previous value, so `CompletionPopup` reads `items.length` directly when resetting its selection.

The theme picker also reuses `PickerDialog`. Opening it sends `themes_list`; item values encode the full typed identity as JSON so an external theme named `light`, `dark`, or `automatic` cannot collide with a built-in. The dialog marks the requested identity current and describes both the requested and effective themes when fallback is active. `BackendBridge.applyThemeState()` validates inventory and diagnostic bounds, ignores older generations, and replaces `themeState` in one assignment. `Theme.completeExternalPalette()` checks every semantic role before any external color is used, preventing mixed built-in/external frames. Backend restart resets only the client generation comparison so the new process can publish generation one.

The command palette reuses `PickerDialog` with a `group` per item. `shell.paletteItems()` builds actions (recent ones first, from `bridge.recentActions`), tabs, models, sessions, Pi commands, and skill files; `openPalette()` presents the list at once and reloads models, sessions, and commands so capability changes are reflected while it is open (`refreshPalette`). Values are prefixed (`action:`, `tab:`, `model:`, `session:`, `command:`, `skill:`); `palettePicked` dispatches them, records actions and commands through `recent_action`, inserts commands into the composer without sending, and confirms skill files before `open_path`. Actions run from a short timer after the picker closes so dialogs they open receive focus. Usage comes from `session_stats`, requested 250 ms after `run.end` and after a snapshot; the shell renders it as the **Usage** group inside `StatusOverlay`. The compact trigger derives its count from the unchanged grouped status model. The popup maps itself above that trigger inside `contentRoot`, bounds its size, wraps values and descriptions without elision, scrolls tall content, closes on Escape or outside presses, and returns focus to the trigger. The notice model keeps 200 entries with a `tab` label and a `noticeRevision` counter that the events and diagnostics dialogs bind to.

Attachments live in the backend (`attachments.mjs`) and the bridge mirrors the list; `sendPrompt` passes the ids and clears the mirror unless the backend refused before consuming them (`busy`, `not_ready`, `not_running`). `FileDialog` results are passed with `granted: true`; every other path is confined to the workspace. Drafts: `draftTimer` (600 ms) saves the composer text under `bridge.draftKey` (session file, else workspace) and `restoreDraft` fills only an empty editor for the current key.
- `qml/SmokeDriver.qml` — loaded only when `QT_WEBUI_SMOKE_MODE=1`.

Transcript rows are flat: one row per user message, text part, thinking part, or tool call, keyed by `rowId`. Streaming updates call `ListModel.setProperty` on the affected row only; `MarkdownBlocks` re-parses that row's `blocksJson` and rebuilds its block delegates. Search runs over row text and tool output and highlights whole rows. Scrolling away from the end clears `followOutput`; the Latest control restores it before positioning the list at the end.

Every control has an accessible role and name; `AppButton` draws a focus ring, activates from the keyboard, and uses theme-owned foreground/background pairs that meet 4.5:1 contrast for 12 px labels. Tabs and list rows reserve focus-border space and paint pressed, keyboard-focus, hover, selected, and idle states in that order. Dialogs are `Popup`s with `modal: true`, `focus: true`, an explicit initial focus item, and focus return to the composer.

The prompt control strip's three-line button presents a non-searchable `DropUpPicker` above the trigger. `composerMenuItems()` exposes Resources, automatic settlement with the current day value, thinking-section visibility, syntax highlighting, desktop notifications, appearance, reduced motion, Events, and Diagnostics by dispatching the existing palette actions after the popup closes; actions already visible in the window are deliberately omitted. The model and thinking buttons call `shell.openModelPicker()` / `openThinkingPicker()`, which load data through the bridge and present separate `DropUpPicker` instances above their buttons. Popup geometry stays inside `contentRoot`, arrow movement never selects, Enter/Space select explicitly, and close returns focus to the trigger. The model list uses the backend's helper-resolved Pi scope; the thinking list uses the active model's RPC levels.

`BackendBridge.orderedModelData()` applies `modelOrder` only when `scope.explicit === true`: exact saved identities still present are emitted first in saved order, then unranked models in the backend/Pi order. `saveModelOrder()` merges the complete reordered current scope before saved identities absent from that scope and truncates only after current identities have priority at `maxModels`. The backend validates, deduplicates, and stores the resulting string list. `shell.modelPickerResult()` opts into reordering only for an explicit scope with at least two rendered entries; `modelsReordered()` repeats the active-tab, picker-generation, open-popup, and item-list guards before saving. This keeps stale callbacks and synthetic partial lists from mutating the preference.

`DropUpPicker.moveItem()` copies and reassigns its source array, restores `currentIndex` by exact value, emits `reordered(values)`, and neither emits `picked` nor closes. The visible **≡** handle uses a targetless `DragHandler`; completion maps its translated row center back to a list index. `Ctrl+Shift+Up` and `Ctrl+Shift+Down` call the same move path. Both paths require the opt-in flag, at least two items, and an empty trimmed filter, preventing visible indexes from diverging from source indexes.

The bridge's `selectModel`, `cycleModel`, `setThinkingLevel`, and `cycleThinkingLevel` refuse while `active` or while a previous model/resource change is still unconfirmed, and re-selecting the current value is a no-op. Each successful response includes the backend's freshly resolved `resources`, which the bridge applies instead of guessing; tab snapshots clear resource state and issue `resources_state` for that tab. Runtime events reach the bridge before the request's response, so anything that chains a second change must wait for `modelActionPending` to clear (the smoke driver does this in `advanceModels`). `compactContext` sets `compacting` until the response arrives; the backend keeps the session busy for the same period.

`ResourceProfilesDialog` drafts one field at one explicit scope and only calls `tools_set`, `skills_set`, or `sampling_set` from **Save**. `present()` issues `resources_state`, so each opening reads Pi Web UI's latest canonical file; **Refresh** repeats that boundary without a background watcher. Tool/skill `listMode` keeps `inherit` (`null`) distinct from `custom` with an empty `listDraft` (`[]`). A custom save submits only draft names present in `visibleInventory`; the canonical updater merges retained unavailable names, so an unchanged or partially edited profile never asks the initiating helper to validate names it cannot see. Sampling sends all seven keys as a patch so blank fields remove only that scope's values; disabled fields retain their draft text and are serialized unchanged, while the backend/helper filter unsupported values from the applied provider payload. `controlsEnabled` requires available complete state, an idle Pi session, and no pending model/resource request. The dialog remains inspectable but all edits and saves fail closed while those conditions are false. `sessionDurability.durable: false` adds an alert that the active override is not saved durably, and the bridge posts the same reason after a successful non-durable session mutation. `AppDialog` returns focus to the composer.

## Launcher-to-QML environment contract

Only these launcher-owned values are passed with the `QT_WEBUI_` prefix:

| Name | Contract |
|---|---|
| `QT_WEBUI_CALLER_CWD` | Absolute caller working directory used for the backend and Pi child. |
| `QT_WEBUI_QML_ENTRY` | Absolute selected `shell.qml` path. |
| `QT_WEBUI_BACKEND_ENTRY` | Absolute backend entry (`lib/backend/main.mjs`). |
| `QT_WEBUI_NODE_EXECUTABLE` | Absolute Node.js executable used to start the backend and the Pi CLI module. |
| `QT_WEBUI_PI_CLI_ENTRY` | Absolute dependency-local Pi CLI module, read by the backend. |
| `QT_WEBUI_DEVELOPMENT_MODE` | `1` for `qt-webui dev`; otherwise `0`. |
| `QT_WEBUI_SYSTEM_COLOR_SCHEME` | Normalized XDG portal result at launch: `dark`, `light`, or `unknown`; the backend emits later valid changes. |
| `QT_WEBUI_DESKTOP_CORNER_RADIUS` | Validated non-negative Hyprland corner radius, or `unknown`. |
| `QT_WEBUI_DESKTOP_EDGE_GAP` | Validated non-negative Hyprland outer gap, or `unknown`. |

Before adding those values, the launcher removes every inherited environment key whose name starts with `QT_WEBUI_`. This prevents caller-controlled variables from activating internal test behavior or overriding launcher-owned paths. The live smoke harness can add its fixture values (`QT_WEBUI_SMOKE_MODE`, `QT_WEBUI_SMOKE_CAPTURE_PATH`, `QT_WEBUI_SMOKE_STATE_PATH`, `QT_WEBUI_THEME_MODE`, `QT_WEBUI_PI_STARTUP_TIMEOUT_MS`, `QT_WEBUI_PI_REQUEST_TIMEOUT_MS`) only through an explicit launcher test seam; the installed `qt-webui` bin never reads that seam from the environment. The backend honors `QT_WEBUI_PI_REQUEST_TIMEOUT_MS` and `debug_crash` only in smoke mode.

Path values must be non-empty, contain no NUL byte, and fit within 16 KiB when UTF-8 encoded. The backend passes the Pi CLI entry and `--mode rpc` as separate process arguments and never interpolates paths, prompts, or dialog values into shell text.

The initial `get_state` response supplies `model.provider`, `model.id`, and `thinkingLevel`; the backend copies bounded strings into `pi.runtime` events and clears them on failed state reads, restarts, and process exits so the window never presents stale runtime metadata.

Qt 6.11 registers host applications with `org.freedesktop.host.portal.Registry` during GUI startup, which can produce a non-fatal warning; the launcher defaults the child to `QT_NO_XDG_DESKTOP_PORTAL=1` while preserving an explicit caller value. The visual root explicitly belongs to `FloatingWindow.contentItem` and the surface is opaque.

## Process and failure behavior

The launcher installs handlers for `SIGINT` and `SIGTERM` only while Quickshell is active and forwards either signal to that child. It removes handlers after an error or close event. Numeric child exit codes pass through; signal exits become the conventional `130` or `143` status when applicable.

A missing Quickshell executable and a missing or malformed package-local Pi CLI both become actionable stderr messages. The launcher never falls back to a global Pi command.

## Source-checkout development

From the package directory in a source checkout, start development mode with native QML reload:

```bash
npm run dev
```

This contributor command runs the checkout's launcher and source QML. Installed users should use `qt-webui dev` instead.

## Validation

Run the focused suite and syntax checks:

```bash
npm test
npm run check
qmllint -I /usr/lib/qt6/qml qml/*.qml qml/components/*.qml qml/dialogs/*.qml
```

Test files:

- `tests/appearance.test.mjs` — bounded portal read/signal parsing, live monitor updates, invalid-value retention, unavailable-monitor fallback, and WCAG AA contrast for every built-in filled-button state.
- `tests/backend-units.test.mjs` — protocol validation at each limit and one over, including the automatic-settlement integer setting; JSONL framing; Markdown policy and bounds; settings permissions; deterministic threshold, rollback-safe restore grace, separate manual/automatic capacity, bounded hashed-state, and path-privacy policy; process-tree escalation; canonical/legacy profile migration, no-resurrection and unavailable-name preservation; sampling ranges/capabilities/payload translation; and direct helper semantics including shared session entries, scoped-model normalization/bounds, append failure, and explicit ephemeral durability.
- `tests/backend-session.test.mjs` — the real backend driven against `tests/fixtures/fake-pi-rpc.mjs`: startup noise, stale and duplicate responses, message parts, dialogs answered exactly once, streaming cadence, provider and tool errors, abort-before-start, transcript limits, protocol violations, silent Pi timeouts, slow-consumer backpressure, settings, scoped model inventory/order/bounds, model/thinking selection and refreshed resources, Pi Web UI-prewritten and Qt-written canonical tool/skill profiles, unavailable-name filtering, all profile scopes, Qt-local unsupported sampling preservation, apply/persistence/rollback failures, early helper errors, command/helper timeouts, Pi exit, durable/ephemeral session results, helper loss, malformed profile fallback, manual compaction, and restart paths.
- `tests/backend-composer.test.mjs` — highlighting tokens and budgets, the composer request validation, command normalization, the state and sequence stores (limits, eviction, ordering, corrupt files), attachment confinement/size/type/exact-once rules, workspace confinement (traversal, symlinks) and indexing (walk and fake `git ls-files`), and one backend round trip covering commands, drafts, completion, sequences, and a prompt carrying attachments.
- `tests/backend-tabs.test.mjs` — the transcript mirror and history translation; session listing (directory encoding cross-checked against the Pi package, bounds, corrupt files); deterministic exact/near-threshold automatic settlement, changed thresholds, idle/running/startup-resume/session-switch open-tab exclusion, canonical identity privacy, no-op write behavior, and unchanged Pi JSONL files; directory listing and creation; Git worktrees on real temporary repositories (spaces, detached HEAD, conflicts, nesting, rollback with a failing `worktree add`, timeouts); the tab/session/directory/worktree request validation; and backend round trips: isolated tabs with tagged events, broader resource reconciliation before an unselected tab's next turn, active affected-tab refusal, unread and input badges, replay on select, busy-close refusal and forced close, rename, last-tab replacement, restore after restart (missing folder skipped, session resumed), session listing/resume/interruption warning/new session, and a worktree opening a new tab.
- `tests/backend-lifecycle.test.mjs` — stdin EOF, `SIGTERM`, `SIGINT`, `SIGKILL`, fatal crash, and restart all reap Pi and a fixture grandchild within `shutdownGraceMs + 2 s`.
- `tests/session-sync.test.mjs` — deterministic watcher topology, event coalescing, open-path polling and acknowledgment, watcher failure fallback, stable compaction-aware snapshots, malformed/partial retention, bounds, and cleanup.
- `tests/session-sync-integration.test.mjs` — fake-Pi backend coverage for catalog invalidation, active/inactive external projection, equal/self-write suppression, active-run deferral, partial retention and retry, same-session rebind ordering/cancellation, the centralized mutation-fence inventory, and backend-owned monitor cleanup.
- `tests/extension.test.mjs` — `/qt-webui-start` registration, current-directory forwarding, argument and error notifications, canonical launch preparation, detached spawn options, and child release.
- `tests/qml-contract.test.mjs` — static contracts for the QML files, including Latest visibility and follow-resume wiring, the collapsed status trigger and bounded full-detail overlay, the available-width rail resize handle and its drag and `Shift+Left` / `Shift+Right` paths, global catalog paging/deduplication/generation and refresh coalescing, Working/Settled grouping, idle-only confirmed settlement, automatic-settlement state/application/wiring/input bounds/cancel and failure behavior/refresh-after-save, compact activity/date labels in both groups, transient workspace filtering and clear/focus behavior, open-tab reuse/new-tab resume, centered conversation, minimum size, response controls below the composer, bounded anchored picker geometry and focus return, violet/periwinkle semantic visual roles, structural frames, tracked short labels, rectangular statuses, accessible landmarks, flat composer treatment, stable focus borders, control interaction states, transcript hierarchy, origin-tab callback rejection, explicit-scope ordering gates, exact identity ranking, bounded current-first persistence merge, filter-disabled drag/keyboard moves, non-selecting reorder signals, resource request fields, durability alerts, null/empty semantics, effective sources, sampling reasons, disabled/bounded/accessibility state, the limit cross-check, and untrusted-content rules.
- `tests/themes.test.mjs` — Pi schema and bundle compatibility, xterm and variable resolution, trust and no-install discovery, bounds, palette completeness and contrast repair, typed collisions, persistence, fallback/recovery, and watcher cleanup.
- `tests/qml-smoke.test.mjs` — real Quickshell on the current Wayland session, running the `SmokeDriver` scenario twice (default and `QT_SCALE_FACTOR=2`) plus a focused model-order scenario, exercising case-insensitive Working/Settled workspace filtering plus empty-query restoration, global catalog load → settle → collapse/expand → restore → new-tab resume → existing-tab reuse, forcing Light and Dark overrides, returning to portal-backed Automatic, toggling reduced motion, asserting every marker, model/thinking drop-up bounds and focus return, a completed keyboard-equivalent reorder through the picker function and shell persistence wiring, saved-order reapplication, delayed resource read/mutation responses across tab switches, the captured Pi/helper commands, tool/skill/sampling profile application, unsupported-value preservation, exact dialog answers, and persisted settings. It skips without a Wayland display or Quickshell.
- `tests/launcher.test.mjs`, `tests/package-contract.test.mjs`, `tests/packed-install.test.mjs`, `tests/docs-contract.test.mjs` — launcher, manifest, packed install, and documentation contracts. Packed-install coverage imports the installed theme service against a package exposing `pi.themes`, requires missing-package handling to return `skip`, accepts inert HTML export fields, and proves the fixture extension was not executed.

`tests/helpers/backend-client.mjs` spawns the backend with the fixture, keeps unmatched responses observable, and rejects pending requests when the backend exits. It isolates `XDG_CONFIG_HOME`, `XDG_STATE_HOME`, and `PI_WEBUI_SETTINGS_FILE` per backend; without those boundaries, test state could restore another run's tabs or read and mutate the developer's real shared resource settings. Tests that list sessions set `PI_CODING_AGENT_DIR` to a temporary agent directory.

The fixture's internal `/qt-webui-helper` command answers bounded state/apply calls, reports empty, explicit, and oversized scoped-model sets, persists session overrides in memory, changes sampling capabilities with the active model, and can simulate missing, silent, delayed, nth-call failed, early-error, ephemeral, and process-exit outcomes. Helper prompts are excluded from the smoke test's user-prompt order before their JSON payloads are asserted separately.

The fixture scenarios are keyed by prompt text (`__QT_WEBUI_STREAM__`, `__QT_WEBUI_MARKDOWN__`, `__QT_WEBUI_IMMEDIATE__`, `__QT_WEBUI_PROVIDER_ERROR__`, `__QT_WEBUI_TOOL_ERROR__`, `__QT_WEBUI_FAIL__`, `__QT_WEBUI_DELAYED_ABORT__`, `__QT_WEBUI_LIMITS__`, `__QT_WEBUI_FLOOD__`, `__QT_WEBUI_GRANDCHILD__`, `__QT_WEBUI_SILENT__`, `__QT_WEBUI_EXIT__`). The startup state file drives the `failed-state` → `missing-state` → `recovered-state` restart sequence. The fixture also answers `get_available_models` (three usable models plus malformed and duplicate entries, or 300 extra with `QT_WEBUI_FIXTURE_MANY_MODELS=1`), `set_model`, `cycle_model`, `get_available_thinking_levels` (with a bogus level), `set_thinking_level`, `cycle_thinking_level`, and `compact` (failing when `customInstructions` is `__QT_WEBUI_COMPACT_FAIL__`).

The fixture answers `get_commands` with three usable commands plus malformed and duplicate entries, keys prompt scenarios on the first line of the message so attachment blocks below the prompt do not change the scenario, tracks the current session file through `switch_session` (cancelled for paths containing `cancel-me` or calls selected by `QT_WEBUI_FIXTURE_CANCEL_SWITCH_AT`), `new_session`, and `set_session_name`, and answers `get_messages` from a complete readable fixture JSONL file before falling back to scripted histories for `resume-me` (complete, with a tool call) or `interrupted` (ending with an unanswered user message). The smoke workspace is a small Git repository with `src/main.mjs`, a sibling `other` folder, and a `resume-me` session file under the temporary `PI_CODING_AGENT_DIR`. Its folder name `<b>project</b>` contains a slash, so the smoke driver derives sibling paths from `QT_WEBUI_SMOKE_STATE_PATH` rather than from the workspace path.

The smoke driver answers dialogs through the dialog's own keyboard-handler entry points (`selectOption`, `confirm`, `submitText`, `cancel`) rather than synthesized key events; a second answer must be refused. Its composer phase drives `Composer.setText`/`acceptCurrentCompletion`, `bridge.addAttachment`, the draft timer, and `SequencesDialog.runCurrent`/`deleteCurrent`, and the smoke test asserts the captured prompt, follow-up, and `get_commands` commands. The ordering scenario invokes the real picker key handler, verifies focus/current selection and persistence, and exercises reapplication through the bridge. Known gap: the harness does not synthesize native pointer drags or key presses, so drag gesture arbitration remains covered by QML lint and static handler contracts rather than coordinate-dependent live input.

`tests/run-all.mjs` runs test files one at a time (`--test-concurrency=1`): the live Quickshell smoke is CPU-heavy and made timing-sensitive backend tests flaky when files ran in parallel.

`tests/packed-install.test.mjs` creates a tarball under a disposable temporary directory, installs it into a temporary prefix with `npm install --ignore-scripts`, adds that prefix's bin directory to `PATH`, invokes `qt-webui` by command name against the fake Quickshell fixture, and removes the directory. It must never install globally or publish the package.

## Packaging

Keep `qml`, `bin`, `lib`, the deterministic `screenshots` asset, user documentation, contributor documentation, tests, and `LICENSE` in the package `files` allowlist. Generate `package-lock.json` with lifecycle scripts disabled:

```bash
npm install --package-lock-only --ignore-scripts
```

Do not publish or change Pi settings as part of repository validation.
